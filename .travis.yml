language: r
cache: packages
sudo: required
warnings_are_errors: false

# blacklist
# branches:
#   except:
#     - master

# whitelist
branches:
  only:
    - master
    - release

r_binary_packages:
  - ape
  - covr
  - devtools
  - knitr
  - phangorn
  - roxygen2
  - shiny
  - testthat

r_packages:
  - TreeSearch

# Code coverage
after_success:
- Rscript -e 'library("covr"); codecov()'
