language: r
warnings_are_errors: false

# blacklist
# branches:
#   except:
#     - master

# whitelist
branches:
  only:
    - master
    # Change to release once released

# Code coverage
r_packages:
  - devtools
  - covr

after_success:
- Rscript -e 'library("covr"); codecov()'
